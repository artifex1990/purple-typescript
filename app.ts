/* interface User {
    name: string
}

interface User {
    age: number
}

const user: User = {
    name: 'john',
    age: 25
} */

type ID = string | number;

interface IDI {
    ID: string | number;
}